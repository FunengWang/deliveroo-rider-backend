server:
  port: 8085
  servlet:
    context-path: /rider

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.gjt.mm.mysql.Driver
    #该服务位于弗吉尼亚州，如果感觉连接很慢，打开VPN
    url: "jdbc:mysql://ec2-54-211-145-203.compute-1.amazonaws.com:3306/rider_db?useUnicode=true&characterEncoding=utf-8&useSSL=false"
    username: "root"
    password: "root"
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    hibernate.ddl-auto: "update"
#    hibernate.ddl-auto: "create-drop"
    show-sql: true

custom:
  places:
    - GAL
    - SHT
  shops:
    - Supermac's
    - McDonald's
    - Subway
    - Burger King
    - Papa John's